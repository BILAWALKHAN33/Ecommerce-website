// Optionally add a delay before the animation starts
setTimeout(function() {
    document.querySelector('.banner').style.animationPlayState = 'running';
  }, 2000); // Adjust the delay in milliseconds (e.g., 2000ms is a 2 second delay)
  